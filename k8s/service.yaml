apiVersion: v1
kind: Service
metadata:
  name: e2e-lane-fastapi-react
  namespace: colossus-e2e-lane-fastapi-react
  labels:
    app: e2e-lane-fastapi-react
    managed-by: colossus
spec:
  type: ClusterIP
  selector:
    app: e2e-lane-fastapi-react
  ports:
    - port: 80
      targetPort: 80
